# 🎨 컬러 팔레트 디자인 시스템

## 📝 프로젝트 정보
- **출처 웹사이트**: Hasselblad (하셀블라드 공식 웹사이트)
- **분석 날짜**: 2025-12-19
- **적용 프로젝트**: 포트폴리오 웹사이트
- **디자인 컨셉**: 프리미엄 럭셔리 / 미니멀리즘 / 다크 모드 중심

---

## 🎯 CSS 변수 정의

```css
:root {
  /* ========================================
     Primary Colors - 주요 브랜드 색상
     ======================================== */
  --color-primary: #C9A962;          /* 골드 브론즈 - 메인 강조색 */
  --color-primary-light: #D4B978;    /* 밝은 골드 - 호버 상태 */
  --color-primary-dark: #A88B4A;     /* 어두운 골드 - 액티브 상태 */

  /* ========================================
     Secondary Colors - 보조 색상
     ======================================== */
  --color-secondary: #F8F8F5;        /* 오프 화이트 - 라이트 섹션 배경 */
  --color-accent: #C9A962;           /* 골드 - 특별 강조 요소 */

  /* ========================================
     Background Colors - 배경 색상
     ======================================== */
  --color-bg-primary: #0D0D0D;       /* 딥 블랙 - 메인 배경 */
  --color-bg-secondary: #1A1A1A;     /* 차콜 - 카드/섹션 배경 */
  --color-bg-tertiary: #2A2A2A;      /* 연한 차콜 - 호버 배경 */
  --color-bg-light: #F8F8F5;         /* 크림색 - 라이트 모드 배경 */
  --color-bg-white: #FFFFFF;         /* 순백색 - 갤러리/푸터 */

  /* ========================================
     Text Colors - 텍스트 색상
     ======================================== */
  --color-text-primary: #FFFFFF;     /* 흰색 - 제목, 중요 텍스트 */
  --color-text-secondary: #A0A0A0;   /* 중간 회색 - 본문 텍스트 */
  --color-text-muted: #666666;       /* 어두운 회색 - 부가 설명 */
  --color-text-dark: #1A1A1A;        /* 검정 - 라이트 모드 텍스트 */

  /* ========================================
     Interactive Colors - 상호작용 요소
     ======================================== */
  --color-button-primary: #FFFFFF;   /* 흰색 버튼 */
  --color-button-hover: #E8E8E8;     /* 버튼 호버 */
  --color-button-accent: #C9A962;    /* 골드 버튼 */
  --color-link: #FFFFFF;             /* 링크 기본 */
  --color-link-hover: #C9A962;       /* 링크 호버 - 골드 */

  /* ========================================
     Border & Divider - 테두리/구분선
     ======================================== */
  --color-border-dark: #2A2A2A;      /* 다크 모드 테두리 */
  --color-border-light: #E0E0E0;     /* 라이트 모드 테두리 */
  --color-divider: #333333;          /* 섹션 구분선 */

  /* ========================================
     Gradients - 그라데이션
     ======================================== */
  --gradient-dark: linear-gradient(180deg, #0D0D0D 0%, #1A1A1A 100%);
  --gradient-gold: linear-gradient(135deg, #C9A962 0%, #D4B978 100%);
  --gradient-overlay: linear-gradient(180deg, rgba(0,0,0,0) 0%, rgba(0,0,0,0.8) 100%);

  /* ========================================
     Shadows - 그림자
     ======================================== */
  --shadow-sm: 0 2px 4px rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.5);
  --shadow-gold: 0 4px 16px rgba(201, 169, 98, 0.3);
}
```

---

## 🎨 컬러 사용 가이드

### 1. Primary Color (골드 브론즈) `#C9A962`
| 사용 위치 | 설명 |
|-----------|------|
| CTA 버튼 | 가장 중요한 행동 유도 버튼 |
| 링크 호버 | 마우스 오버 시 강조 효과 |
| 아이콘 하이라이트 | 특별한 아이콘 강조 |
| 로고/브랜드 요소 | 브랜드 아이덴티티 표현 |
| 진행 상태 표시 | 프로그레스 바, 로딩 인디케이터 |

```jsx
// MUI sx 사용 예시
<Button sx={{
  bgcolor: 'var(--color-primary)',
  '&:hover': { bgcolor: 'var(--color-primary-light)' }
}}>
  문의하기
</Button>
```

### 2. Background Colors (배경색)
| 색상 | Hex | 사용 위치 |
|------|-----|-----------|
| 딥 블랙 | `#0D0D0D` | 메인 페이지 배경, 히어로 섹션 |
| 차콜 | `#1A1A1A` | 카드 배경, 네비게이션, 푸터 |
| 연한 차콜 | `#2A2A2A` | 호버 상태, 선택된 항목 |
| 오프 화이트 | `#F8F8F5` | About 섹션, 대비가 필요한 영역 |

```jsx
// 섹션별 배경색 적용
<Box sx={{ bgcolor: 'var(--color-bg-primary)' }}>  {/* 메인 */}
<Box sx={{ bgcolor: 'var(--color-bg-secondary)' }}> {/* 카드 */}
<Box sx={{ bgcolor: 'var(--color-bg-light)' }}>     {/* 라이트 섹션 */}
```

### 3. Text Colors (텍스트 색상)
| 색상 | Hex | 사용 위치 |
|------|-----|-----------|
| 흰색 | `#FFFFFF` | 제목(h1-h3), 중요 텍스트, 버튼 텍스트 |
| 중간 회색 | `#A0A0A0` | 본문 텍스트, 설명문 |
| 어두운 회색 | `#666666` | 날짜, 메타 정보, 캡션 |

```jsx
// Typography 적용 예시
<Typography variant="h1" sx={{ color: 'var(--color-text-primary)' }}>
  제목
</Typography>
<Typography variant="body1" sx={{ color: 'var(--color-text-secondary)' }}>
  본문 내용
</Typography>
```

### 4. 대비율 가이드 (WCAG 2.1 기준)
| 조합 | 대비율 | 접근성 등급 |
|------|--------|-------------|
| `#FFFFFF` on `#0D0D0D` | 21:1 | AAA ✅ |
| `#A0A0A0` on `#0D0D0D` | 7.5:1 | AAA ✅ |
| `#C9A962` on `#0D0D0D` | 8.2:1 | AAA ✅ |
| `#666666` on `#0D0D0D` | 4.1:1 | AA ✅ |

---

## 📱 반응형 고려사항

### 다크 모드 / 라이트 모드 전환

```css
/* 라이트 모드 오버라이드 */
[data-theme="light"] {
  --color-bg-primary: #FFFFFF;
  --color-bg-secondary: #F8F8F5;
  --color-bg-tertiary: #EEEEEE;
  --color-text-primary: #1A1A1A;
  --color-text-secondary: #666666;
  --color-text-muted: #999999;
  --color-border-dark: #E0E0E0;
}

/* 시스템 설정 따르기 */
@media (prefers-color-scheme: light) {
  :root:not([data-theme="dark"]) {
    --color-bg-primary: #FFFFFF;
    --color-bg-secondary: #F8F8F5;
    --color-text-primary: #1A1A1A;
    --color-text-secondary: #666666;
  }
}
```

### MUI 테마 설정

```javascript
// theme.js
import { createTheme } from '@mui/material/styles';

export const hasselbladTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#C9A962',
      light: '#D4B978',
      dark: '#A88B4A',
      contrastText: '#0D0D0D',
    },
    secondary: {
      main: '#F8F8F5',
      contrastText: '#1A1A1A',
    },
    background: {
      default: '#0D0D0D',
      paper: '#1A1A1A',
    },
    text: {
      primary: '#FFFFFF',
      secondary: '#A0A0A0',
      disabled: '#666666',
    },
    divider: '#2A2A2A',
  },
  typography: {
    fontFamily: '"Pretendard", "Noto Sans KR", sans-serif',
    h1: {
      color: '#FFFFFF',
      fontWeight: 700,
    },
    body1: {
      color: '#A0A0A0',
    },
  },
  components: {
    MuiButton: {
      styleOverrides: {
        root: {
          borderRadius: 0,
          textTransform: 'none',
        },
        contained: {
          backgroundColor: '#FFFFFF',
          color: '#0D0D0D',
          '&:hover': {
            backgroundColor: '#C9A962',
            color: '#0D0D0D',
          },
        },
        outlined: {
          borderColor: '#FFFFFF',
          color: '#FFFFFF',
          '&:hover': {
            borderColor: '#C9A962',
            color: '#C9A962',
          },
        },
      },
    },
    MuiCard: {
      styleOverrides: {
        root: {
          backgroundColor: '#1A1A1A',
          borderRadius: 0,
        },
      },
    },
  },
});
```

### 화면 크기별 색상 조정

```jsx
// 모바일에서 가독성을 위한 조정
<Typography
  sx={{
    color: {
      xs: 'var(--color-text-primary)',    // 모바일: 흰색 (가독성)
      md: 'var(--color-text-secondary)',  // 데스크톱: 회색 (세련됨)
    },
  }}
>
  본문 텍스트
</Typography>
```

---

## 🔧 실제 적용 예시

### 네비게이션 바
```jsx
<AppBar sx={{
  bgcolor: 'transparent',
  backdropFilter: 'blur(10px)',
  borderBottom: '1px solid var(--color-border-dark)',
}}>
```

### 히어로 섹션
```jsx
<Box sx={{
  bgcolor: 'var(--color-bg-primary)',
  background: 'var(--gradient-dark)',
  minHeight: '100vh',
}}>
```

### 카드 컴포넌트
```jsx
<Card sx={{
  bgcolor: 'var(--color-bg-secondary)',
  border: '1px solid var(--color-border-dark)',
  '&:hover': {
    bgcolor: 'var(--color-bg-tertiary)',
    borderColor: 'var(--color-primary)',
  },
}}>
```

### 골드 액센트 버튼
```jsx
<Button sx={{
  bgcolor: 'var(--color-primary)',
  color: 'var(--color-bg-primary)',
  boxShadow: 'var(--shadow-gold)',
  '&:hover': {
    bgcolor: 'var(--color-primary-light)',
  },
}}>
```

---

## 📋 체크리스트

- [ ] CSS 변수를 `index.css`에 추가
- [ ] MUI 테마를 `theme.js`에 설정
- [ ] ThemeProvider로 앱 전체 감싸기
- [ ] 각 컴포넌트에 색상 변수 적용
- [ ] 다크/라이트 모드 전환 테스트
- [ ] 접근성 대비율 검증
